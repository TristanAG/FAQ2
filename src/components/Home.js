import React from 'react'

function Home() {
  // const { firebase, user } = React.useContext(FirebaseContext)

  return (
    <div className="home">
      <p>homepage...</p>
    </div>
  )
}

export default Home
